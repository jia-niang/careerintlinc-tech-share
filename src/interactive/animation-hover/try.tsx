import { Button, Space } from 'antd'

export default function () {
  return (
    <Space wrap>
      <Button type="primary">我是按钮</Button>
      <Button type="primary">我也是按钮</Button>
      <Button type="primary">我又是按钮</Button>
      <Button type="primary">我还是按钮</Button>
      <Button type="primary">我仍是按钮</Button>
      <Button type="primary">我就是按钮</Button>
      <Button type="primary">我总是按钮</Button>
    </Space>
  )
}
